header.header
    .header_column
        a(href=routers.home)
            i.fab.fa-youtube.fa-3x
    .header_column
        form(action=routers.search, method="get")
            input(type="text", placeholder="Search by term...", name="term")
    .header_column
        ul
            if !users.isAuthenticated
                li
                    a(href=routers.join) Join
                li
                    a(href=routers.login) Log In
            else
                li
                    a(href=routers.upload) Upload
                li
                    a(href=routers.userDetail(users.id)) ProFile
                li
                    a(href=routers.logout) Logout

